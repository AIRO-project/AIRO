import { css } from "styled-components";

const shadows = css`
  :root {
    --shadow-sm: 0 0.1rem 0.2rem rgba(0, 0, 0, 0.5);
    --shadow-md: 0 1rem 1rem rgba(0, 0, 0, 0.15);
  }
`;

export default shadows;
